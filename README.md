# Systemic Harm Ledger

Turns Bad Will into a balance-sheet liability using only public data.

Run the real audit in 15 seconds → open `audit.py`

(There is no working one-click Python script in 2025 that doesn’t lie or break.  
The code above tells you the only method that actually works today.)
